/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/Lillia-Landing-Page-Concept',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
